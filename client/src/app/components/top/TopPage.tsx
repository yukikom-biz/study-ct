import * as React from 'react';
import LP from "./LP";

export default function TopPage() {
    // return withAuthenticator(() => <Redirect to="/mypage" />, [TargetLP]);
    // todo: auth を導入したらこんな感じで実装する

    return <LP/>;
}
